---
deployment:
  tasks:
    - export DEPLOYPATH=/home/dovxapev/public_html/
    - /bin/mkdir -p $DEPLOYPATH
    - /bin/cp -R index.html $DEPLOYPATH
    - /bin/cp -R css $DEPLOYPATH
    - /bin/cp -R img $DEPLOYPATH
    - /bin/cp -R pages $DEPLOYPATH
    - /bin/cp -R script $DEPLOYPATH
    - /bin/cp -R SVG $DEPLOYPATH
